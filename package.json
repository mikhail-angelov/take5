{
  "name": "take5",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Node-only dev tooling for take5: the extension icons, the manual acceptance app, the SPIKE.md re-verification harness, and the fixture corpus generator. The product itself is the Go module at the repo root — see cmd/take5.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fixture": "node test/fixtures/serve.js",
    "spike": "node test/spike/run.mjs",
    "icons": "node tools/make-icons.mjs",
    "fixtures:gen": "node tools/gen-fixtures.mjs"
  }
}
